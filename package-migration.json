{
  "name": "invoice-migration",
  "version": "1.0.0",
  "description": "Migration script for moving localStorage data to Firebase Firestore",
  "main": "migrate-to-firestore.js",
  "scripts": {
    "migrate": "node migrate-to-firestore.js"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0"
  }
}
